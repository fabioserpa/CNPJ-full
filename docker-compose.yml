version: '3.7'

services:
  cnpj-full:
    container_name: cnpj-full
    hostname: cnpj-full
    image: python/cnpj-full
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - ./app:/app